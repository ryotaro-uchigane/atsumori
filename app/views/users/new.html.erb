<h1>ユーザー登録</h1>

<%= form_for(@user) do |f| %>
  <%= f.label :email %>
  <%= f.email_field :email %>

  <%= f.label :password %>
  <%= f.password_field :password %>

  <%= f.select :role, {'学生' => 'job_hunting', '企業' => 'company'}%>

  <%= f.submit "登録" %>
<% end %>
