<%= form_for(@event) do |f| %>
  <%= f.text_field :title %>
  <%= f.text_area :description %>
  <%= f.check_box :public %>
  <%= f.label :public %>
  <%= f.submit "保存" %>
  <%= link_to "削除", event_path(@event.id), method: :delete%>
<% end %>
