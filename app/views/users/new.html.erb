<h1>ユーザー登録</h1>

<%= form_for(@user) do |f| %>
  <%= f.label :email %>
  <%= f.email_field :email %>

  <%= f.label :password %>
  <%= f.password_field :password %>

  <%= f.hidden_field :role, value: @user.role %>

  <%= f.submit "登録" %>
<% end %>
